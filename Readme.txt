This code is for analysing the results of an experiments containing single-pulse, double-pulses, and triple-pulses
RDM stimuli.

In order to run the code put the all data in a folder and gives its address to the variable "add" in the code.